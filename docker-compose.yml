version: '3.8'
services:
  frontend:
    build: ./client
    ports:
      - "3000:80" # React app served via nginx

  backend:
    build: ./server
    ports:
      - "5000:5000"
    environment:
      - MONGO_URI=mongodb+srv://harsheel:harsheel1012@harsheel.kpdvl.mongodb.net/
    depends_on:
      - mongo

  mongo:
    image: mongo:5.0
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
